# Architecture Notes

This lab simulates a basic SOC environment with centralized log collection.

Components are separated into:

* Endpoint (Windows)
* SIEM Server (Ubuntu)

Data flows from endpoint telemetry to centralized analysis.

This architecture reflects real-world SIEM deployments.
